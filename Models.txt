ToDo:
- Colour detection model
- Object detection model (Clothes + wallet + keys + Mobile +++)
- Face recognition model
- Car detection model

Notes:
- Make every model has its own options to pass them as arguments


Bouns:
- Add hand written in Image-to-text